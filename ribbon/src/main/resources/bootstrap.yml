eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8801/eureka/
spring:
  cloud:
    config:
      profile: dev
#      uri: http://localhost:8807
      name: ribbon
      discovery:
        service-id: config-server
        enabled: true

